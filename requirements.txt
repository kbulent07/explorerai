# FaceZoom bagimliliklari - SABITLENMIS surumler
# -----------------------------------------------------------------------------
# Bu surumler Python 3.12 ile DOGRULANMIS calisan ortamdan (pip freeze) alindi.
# mediapipe/numpy/insightface/onnxruntime zinciri surume cok duyarlidir; bu
# yuzden pinlenmistir. Guncellemek isterseniz once izole bir ortamda deneyin.
# -----------------------------------------------------------------------------
opencv-python==4.13.0.92
mediapipe==0.10.35
numpy==2.5.0
pyyaml==6.0.3
ruamel.yaml==0.19.1
flask==3.1.3
# Yuz tanima (ArcFace embedding) - kimlik yalniz bellekte tutulur
insightface==1.0.1
onnxruntime==1.27.0
# Production WSGI server + sistem kaynak olcumu (bellek butcesi)
waitress==3.0.2
psutil==7.2.2
